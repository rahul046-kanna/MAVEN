name: Test Self-Hosted Runner

on: [push]

jobs:
  test-runner:
    runs-on: self-hosted   # 👈 this ensures it runs on your machine
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Say Hello
        run: echo "✅ Hello from my self-hosted runner!"

      - name: Show system info
        run: |
          echo "Running on: $env:COMPUTERNAME"
          echo "OS: $env:OS"
